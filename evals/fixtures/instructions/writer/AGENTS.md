# Writer rules

Keep file creation exclusive. Do not add a fallback that overwrites an existing output. The root test command exercises the CLI entry path. Product behavior belongs in `docs/product.md`.
