# Plain text export

The CLI writes the requested content to a caller-supplied output path. The parent directory must already exist. Existing output files are rejected to preserve user work. Changing overwrite behavior requires an agreed feature plan. This repository has no iOS application, backend, or planned feature change.
